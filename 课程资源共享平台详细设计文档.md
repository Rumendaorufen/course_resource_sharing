# 课程资源共享平台详细设计文档

## 1. 引言

### 1.1 文档目的

本详细设计文档旨在为课程资源共享平台的开发团队提供详细的实施指导。文档基于需求规格说明和概要设计，进一步细化了系统的各个方面，包括总体架构、模块设计、数据库设计、接口设计、安全设计、性能设计、部署设计和测试计划等，确保开发团队能够按照文档进行系统实现，保证系统的质量和一致性。

### 1.2 文档范围

本文档覆盖了课程资源共享平台的所有核心功能模块，包括：

1. 用户管理模块
2. 课程管理模块
3. 资源管理模块
4. 作业管理模块
5. 数据可视化模块
6. MongoDB双写模块

文档详细描述了每个模块的设计实现、数据库设计、接口设计、安全设计、性能设计、部署设计和测试计划，为开发团队提供了完整的实施指导。

### 1.3 读者对象

| 读者类型 | 职责 | 关注点 |
|---------|------|--------|
| 开发人员 | 系统开发和实现 | 模块设计、接口定义、数据库设计、安全设计 |
| 测试人员 | 系统测试和验证 | 测试计划、功能点、异常处理 |
| 项目经理 | 项目管理和协调 | 项目范围、进度控制、资源分配 |
| 系统管理员 | 系统部署和维护 | 部署设计、性能设计、安全设计 |
| 架构师 | 系统架构设计 | 总体架构、模块划分、技术选型 |

### 1.4 术语定义

| 术语 | 解释 |
|------|------|
| JWT | JSON Web Token，一种用于身份认证的令牌 |
| RESTful API | 基于REST架构风格的API设计 |
| 双写机制 | 数据同时写入两个不同的数据库，确保数据一致性 |
| 二级缓存 | 一级缓存（Caffeine）+ 二级缓存（Redis）的架构 |
| 高内聚 | 模块内部功能紧密相关 |
| 低耦合 | 模块之间通过接口通信，减少直接依赖 |
| 微服务 | 一种架构风格，将系统拆分为多个独立的服务 |
| 前后端分离 | 前端和后端代码分离开发，通过API通信 |

## 2. 总体架构

### 2.1 系统层次结构

课程资源共享平台采用前后端分离的微服务架构，主要分为以下几个层次：

1. **前端应用层**：基于Vue 3 + Element Plus开发，负责用户界面展示和交互
2. **API网关层**：处理请求路由、负载均衡、认证授权等
3. **后端服务层**：基于Spring Boot开发，提供RESTful API服务
4. **数据访问层**：基于MyBatis-Plus和Spring Data MongoDB开发，负责数据持久化
5. **数据库层**：包括MySQL和MongoDB，实现数据双写
6. **缓存层**：包括Caffeine和Redis，实现二级缓存架构
7. **文件存储层**：本地文件系统，用于存储上传的文件资源

### 2.2 模块划分

系统划分为以下核心模块：

| 模块名称 | 主要职责 | 技术实现 |
|---------|---------|----------|
| 用户管理 | 用户注册、登录、认证、权限管理 | Spring Security + JWT |
| 课程管理 | 课程创建、编辑、管理、学生管理 | Spring Boot + MyBatis-Plus |
| 资源管理 | 资源上传、下载、分类、搜索、预览 | Spring Boot + MongoDB双写 |
| 作业管理 | 作业发布、提交、批改、评分 | Spring Boot + MongoDB双写 |
| 数据可视化 | 资源分布、作业完成情况统计 | ECharts |
| MongoDB双写 | MySQL和MongoDB数据双写实现 | Spring Data MongoDB |

### 2.3 核心组件交互关系

#### 2.3.1 用户认证流程

```
用户 → 前端登录页面 → 提交登录请求 → 后端AuthController → 验证用户信息 → 生成JWT Token → 返回Token → 前端存储Token → 后续请求携带Token
```

#### 2.3.2 资源访问流程

```
前端组件 → 发起资源访问请求 → 添加JWT Token → 后端验证Token → 查询多级缓存 → 缓存未命中查询数据库 → 返回结果 → 写入缓存
```

#### 2.3.3 文件上传流程

```
前端上传组件 → 提交文件 → 后端FileController → 存储文件 → 记录元数据 → MongoDB双写 → 更新缓存 → 返回文件URL
```

#### 2.3.4 双写机制流程

```
业务服务 → 生成mongoId → 写入MongoDB → 写入MySQL → 成功返回 → 失败处理（记录垃圾数据）
```

## 3. 模块设计

### 3.1 用户管理模块

#### 3.1.1 模块概述

用户管理模块负责用户注册、登录、认证、权限管理等功能，支持管理员、教师、学生三种角色。

#### 3.1.2 类图

```
├── UserController           # 用户管理控制器
├── UserService              # 用户管理服务接口
├── UserServiceImpl          # 用户管理服务实现
├── UserMapper               # 用户数据访问接口
├── User                     # 用户实体类
├── UserDTO                  # 用户数据传输对象
├── UserVO                   # 用户视图对象
├── RoleController           # 角色管理控制器
├── RoleService              # 角色管理服务接口
├── RoleServiceImpl          # 角色管理服务实现
├── RoleMapper               # 角色数据访问接口
├── Role                     # 角色实体类
├── PermissionController     # 权限管理控制器
├── PermissionService        # 权限管理服务接口
├── PermissionServiceImpl    # 权限管理服务实现
├── PermissionMapper         # 权限数据访问接口
└── Permission               # 权限实体类
```

#### 3.1.3 核心接口

| 接口名称 | 功能描述 | 输入 | 输出 | 异常处理 |
|---------|---------|------|------|----------|
| register | 用户注册 | UserDTO（username, password, email, realName, role） | UserVO | 用户名已存在异常、邮箱已存在异常、密码强度不足异常 |
| login | 用户登录 | LoginDTO（username, password） | TokenVO（token, refreshToken, expireTime） | 用户名或密码错误异常 |
| getUserInfo | 获取用户信息 | Long userId | UserVO | 用户不存在异常 |
| updateUser | 更新用户信息 | Long userId, UserDTO | UserVO | 用户不存在异常、权限不足异常 |
| changePassword | 修改密码 | ChangePasswordDTO（oldPassword, newPassword） | Boolean | 旧密码错误异常、密码强度不足异常 |
| resetPassword | 重置密码 | String email | Boolean | 邮箱不存在异常 |
| getRoles | 获取角色列表 | 无 | List<RoleVO> | 无 |
| assignPermission | 分配权限 | Long roleId, List<Long> permissionIds | Boolean | 角色不存在异常、权限不存在异常 |

#### 3.1.4 处理逻辑

##### 3.1.4.1 用户注册流程

1. 接收注册请求，验证输入参数
2. 检查用户名和邮箱是否已存在
3. 验证密码强度（至少8位，包含大小写字母和数字）
4. 对密码进行BCrypt加密
5. 创建用户记录，设置默认角色
6. 返回注册成功信息

##### 3.1.4.2 用户登录流程

1. 接收登录请求，验证输入参数
2. 根据用户名查询用户信息
3. 验证密码是否正确
4. 生成JWT Token（有效期24小时）
5. 生成刷新Token（有效期7天）
6. 返回Token信息

##### 3.1.4.3 权限验证流程

1. 前端请求携带JWT Token
2. 后端JWT过滤器验证Token
3. 从Token中提取用户信息和权限
4. 检查用户是否有权限访问当前资源
5. 有权限则继续处理请求，否则返回403 Forbidden

#### 3.1.5 异常处理

| 异常类型 | 异常描述 | 处理方式 |
|---------|---------|----------|
| UsernameExistsException | 用户名已存在 | 返回400 Bad Request，提示用户名已存在 |
| EmailExistsException | 邮箱已存在 | 返回400 Bad Request，提示邮箱已存在 |
| PasswordStrengthException | 密码强度不足 | 返回400 Bad Request，提示密码强度不足 |
| InvalidCredentialsException | 用户名或密码错误 | 返回401 Unauthorized，提示用户名或密码错误 |
| UserNotFoundException | 用户不存在 | 返回404 Not Found，提示用户不存在 |
| PermissionDeniedException | 权限不足 | 返回403 Forbidden，提示权限不足 |

### 3.2 课程管理模块

#### 3.2.1 模块概述

课程管理模块负责课程创建、编辑、管理、学生管理等功能，支持管理员和教师两种角色操作。

#### 3.2.2 类图

```
├── CourseController         # 课程管理控制器
├── CourseService            # 课程管理服务接口
├── CourseServiceImpl        # 课程管理服务实现
├── CourseMapper             # 课程数据访问接口
├── Course                   # 课程实体类
├── CourseDTO                # 课程数据传输对象
├── CourseVO                 # 课程视图对象
├── CourseCategoryController # 课程分类控制器
├── CourseCategoryService    # 课程分类服务接口
├── CourseCategoryServiceImpl # 课程分类服务实现
├── CourseCategoryMapper     # 课程分类数据访问接口
├── CourseCategory           # 课程分类实体类
├── StudentCourseController  # 学生课程控制器
├── StudentCourseService     # 学生课程服务接口
├── StudentCourseServiceImpl # 学生课程服务实现
├── StudentCourseMapper      # 学生课程数据访问接口
└── StudentCourse            # 学生课程实体类
```

#### 3.2.3 核心接口

| 接口名称 | 功能描述 | 输入 | 输出 | 异常处理 |
|---------|---------|------|------|----------|
| createCourse | 创建课程 | CourseDTO（name, description, teacherId, categoryId） | CourseVO | 教师不存在异常、分类不存在异常 |
| updateCourse | 更新课程 | Long courseId, CourseDTO | CourseVO | 课程不存在异常、权限不足异常 |
| deleteCourse | 删除课程 | Long courseId | Boolean | 课程不存在异常、权限不足异常 |
| getCourseById | 获取课程详情 | Long courseId | CourseVO | 课程不存在异常 |
| getCourseList | 获取课程列表 | CourseQueryDTO（page, size, name, teacherId, categoryId） | PageResult<CourseVO> | 无 |
| addStudentToCourse | 添加学生到课程 | Long courseId, List<Long> studentIds | Boolean | 课程不存在异常、学生不存在异常、权限不足异常 |
| removeStudentFromCourse | 从课程移除学生 | Long courseId, Long studentId | Boolean | 课程不存在异常、学生不存在异常、权限不足异常 |
| getCourseStudents | 获取课程学生列表 | Long courseId | List<UserVO> | 课程不存在异常、权限不足异常 |

#### 3.2.4 处理逻辑

##### 3.2.4.1 创建课程流程

1. 接收创建课程请求，验证输入参数
2. 验证教师和分类是否存在
3. 检查当前用户是否有权限创建课程
4. 创建课程记录，设置默认状态为启用
5. 返回创建成功信息

##### 3.2.4.2 更新课程流程

1. 接收更新课程请求，验证输入参数
2. 验证课程是否存在
3. 检查当前用户是否有权限更新课程
4. 更新课程信息
5. 返回更新成功信息

##### 3.2.4.3 添加学生到课程流程

1. 接收添加学生请求，验证输入参数
2. 验证课程和学生是否存在
3. 检查当前用户是否有权限管理该课程
4. 添加学生到课程
5. 返回添加成功信息

#### 3.2.5 异常处理

| 异常类型 | 异常描述 | 处理方式 |
|---------|---------|----------|
| CourseNotFoundException | 课程不存在 | 返回404 Not Found，提示课程不存在 |
| TeacherNotFoundException | 教师不存在 | 返回404 Not Found，提示教师不存在 |
| CategoryNotFoundException | 分类不存在 | 返回404 Not Found，提示分类不存在 |
| StudentNotFoundException | 学生不存在 | 返回404 Not Found，提示学生不存在 |
| PermissionDeniedException | 权限不足 | 返回403 Forbidden，提示权限不足 |

### 3.3 资源管理模块

#### 3.3.1 模块概述

资源管理模块负责资源上传、下载、分类、搜索、预览等功能，支持资源的MySQL和MongoDB双写存储。

#### 3.3.2 类图

```
├── ResourceController       # 资源管理控制器
├── ResourceService          # 资源管理服务接口
├── ResourceServiceImpl      # 资源管理服务实现
├── ResourceMapper           # 资源数据访问接口
├── Resource                 # 资源实体类
├── ResourceDTO              # 资源数据传输对象
├── ResourceVO               # 资源视图对象
├── ResourceDocument         # 资源MongoDB文档
├── FileController           # 文件上传下载控制器
├── FileService              # 文件服务接口
├── FileServiceImpl          # 文件服务实现
└── MongoDbService           # MongoDB操作服务接口
```

#### 3.3.3 核心接口

| 接口名称 | 功能描述 | 输入 | 输出 | 异常处理 |
|---------|---------|------|------|----------|
| uploadResource | 上传资源 | MultipartFile file, ResourceDTO（name, description, courseId, type） | ResourceVO | 课程不存在异常、文件上传失败异常 |
| downloadResource | 下载资源 | Long resourceId | ResourceVO | 资源不存在异常、权限不足异常 |
| getResourceById | 获取资源详情 | Long resourceId | ResourceVO | 资源不存在异常 |
| updateResource | 更新资源 | Long resourceId, ResourceDTO | ResourceVO | 资源不存在异常、权限不足异常 |
| deleteResource | 删除资源 | Long resourceId | Boolean | 资源不存在异常、权限不足异常 |
| searchResources | 搜索资源 | ResourceQueryDTO（page, size, keyword, courseId, type） | PageResult<ResourceVO> | 无 |
| previewResource | 预览资源 | Long resourceId | ResourcePreviewVO | 资源不存在异常、不支持预览格式异常 |
| incrementDownloadCount | 增加下载次数 | Long resourceId | Boolean | 资源不存在异常 |

#### 3.3.4 处理逻辑

##### 3.3.4.1 资源上传流程

1. 接收上传请求，验证输入参数
2. 验证课程是否存在
3. 生成唯一文件名和存储路径
4. 存储文件到本地文件系统
5. 生成mongoId
6. 创建ResourceDocument并写入MongoDB
7. 创建Resource实体并写入MySQL
8. 更新相关缓存
9. 返回资源信息

##### 3.3.4.2 资源下载流程

1. 接收下载请求，验证资源是否存在
2. 检查用户是否有权限下载该资源
3. 增加资源下载次数
4. 返回资源文件流

##### 3.3.4.3 资源更新流程

1. 接收更新请求，验证输入参数
2. 验证资源是否存在
3. 检查用户是否有权限更新该资源
4. 生成新的mongoId
5. 创建新的ResourceDocument并写入MongoDB
6. 更新Resource实体并写入MySQL
7. 删除旧的ResourceDocument（带重试）
8. 更新相关缓存
9. 返回更新后的资源信息

#### 3.3.5 异常处理

| 异常类型 | 异常描述 | 处理方式 |
|---------|---------|----------|
| ResourceNotFoundException | 资源不存在 | 返回404 Not Found，提示资源不存在 |
| CourseNotFoundException | 课程不存在 | 返回404 Not Found，提示课程不存在 |
| FileUploadException | 文件上传失败 | 返回500 Internal Server Error，提示文件上传失败 |
| PermissionDeniedException | 权限不足 | 返回403 Forbidden，提示权限不足 |
| UnsupportedPreviewFormatException | 不支持预览格式 | 返回400 Bad Request，提示不支持预览该格式 |

### 3.4 作业管理模块

#### 3.4.1 模块概述

作业管理模块负责作业发布、提交、批改、评分等功能，支持教师发布作业，学生提交作业，教师批改作业。

#### 3.4.2 类图

```
├── AssignmentController         # 作业管理控制器
├── AssignmentService            # 作业管理服务接口
├── AssignmentServiceImpl        # 作业管理服务实现
├── AssignmentMapper             # 作业数据访问接口
├── Assignment                   # 作业实体类
├── AssignmentDTO                # 作业数据传输对象
├── AssignmentVO                 # 作业视图对象
├── AssignmentDocument           # 作业MongoDB文档
├── HomeworkSubmissionController # 作业提交控制器
├── HomeworkSubmissionService    # 作业提交服务接口
├── HomeworkSubmissionServiceImpl # 作业提交服务实现
├── HomeworkSubmissionMapper     # 作业提交数据访问接口
├── HomeworkSubmission           # 作业提交实体类
├── HomeworkSubmissionDTO        # 作业提交数据传输对象
├── HomeworkSubmissionVO         # 作业提交视图对象
└── SubmissionDocument           # 作业提交MongoDB文档
```

#### 3.4.3 核心接口

| 接口名称 | 功能描述 | 输入 | 输出 | 异常处理 |
|---------|---------|------|------|----------|
| createAssignment | 创建作业 | AssignmentDTO（title, description, content, courseId, deadline, maxScore） | AssignmentVO | 课程不存在异常、权限不足异常 |
| updateAssignment | 更新作业 | Long assignmentId, AssignmentDTO | AssignmentVO | 作业不存在异常、权限不足异常 |
| deleteAssignment | 删除作业 | Long assignmentId | Boolean | 作业不存在异常、权限不足异常 |
| getAssignmentById | 获取作业详情 | Long assignmentId | AssignmentVO | 作业不存在异常 |
| getAssignmentsByCourse | 获取课程作业列表 | Long courseId | List<AssignmentVO> | 课程不存在异常 |
| submitHomework | 提交作业 | HomeworkSubmissionDTO（assignmentId, content, attachmentUrl, attachmentName, attachmentSize） | HomeworkSubmissionVO | 作业不存在异常、已过截止日期异常 |
| gradeHomework | 批改作业 | Long submissionId, GradeDTO（score, comment, feedback） | HomeworkSubmissionVO | 提交不存在异常、权限不足异常 |
| getSubmissionsByAssignment | 获取作业提交列表 | Long assignmentId | List<HomeworkSubmissionVO> | 作业不存在异常、权限不足异常 |
| getSubmissionById | 获取作业提交详情 | Long submissionId | HomeworkSubmissionVO | 提交不存在异常、权限不足异常 |

#### 3.4.4 处理逻辑

##### 3.4.4.1 作业发布流程

1. 接收发布请求，验证输入参数
2. 验证课程是否存在
3. 检查当前用户是否有权限发布作业
4. 生成mongoId
5. 创建AssignmentDocument并写入MongoDB
6. 创建Assignment实体并写入MySQL
7. 更新相关缓存
8. 返回作业信息

##### 3.4.4.2 作业提交流程

1. 接收提交请求，验证输入参数
2. 验证作业是否存在
3. 检查是否已过截止日期
4. 生成mongoId
5. 创建SubmissionDocument并写入MongoDB
6. 创建HomeworkSubmission实体并写入MySQL
7. 更新相关缓存
8. 返回提交信息

##### 3.4.4.3 作业批改流程

1. 接收批改请求，验证输入参数
2. 验证提交是否存在
3. 检查当前用户是否有权限批改该作业
4. 更新作业提交评分和评语
5. 更新MongoDB中的SubmissionDocument
6. 更新MySQL中的HomeworkSubmission实体
7. 更新相关缓存
8. 返回批改后的提交信息

#### 3.4.5 异常处理

| 异常类型 | 异常描述 | 处理方式 |
|---------|---------|----------|
| AssignmentNotFoundException | 作业不存在 | 返回404 Not Found，提示作业不存在 |
| SubmissionNotFoundException | 提交不存在 | 返回404 Not Found，提示提交不存在 |
| CourseNotFoundException | 课程不存在 | 返回404 Not Found，提示课程不存在 |
| DeadlinePassedException | 已过截止日期 | 返回400 Bad Request，提示已过截止日期 |
| PermissionDeniedException | 权限不足 | 返回403 Forbidden，提示权限不足 |

### 3.5 数据可视化模块

#### 3.5.1 模块概述

数据可视化模块负责展示资源分布、作业完成情况等统计数据，使用ECharts实现图表展示。

#### 3.5.2 类图

```
├── DashboardController         # 数据可视化控制器
├── DashboardService            # 数据可视化服务接口
├── DashboardServiceImpl        # 数据可视化服务实现
├── AnalyticsController         # 分析报告控制器
├── AnalyticsService            # 分析报告服务接口
├── AnalyticsServiceImpl        # 分析报告服务实现
├── ChartService                # 图表生成服务接口
└── ChartServiceImpl            # 图表生成服务实现
```

#### 3.5.3 核心接口

| 接口名称 | 功能描述 | 输入 | 输出 | 异常处理 |
|---------|---------|------|------|----------|
| getResourceDistribution | 获取资源分布统计 | ResourceDistributionQueryDTO（courseId, startDate, endDate） | ResourceDistributionVO | 无 |
| getAssignmentCompletion | 获取作业完成情况 | AssignmentCompletionQueryDTO（courseId, assignmentId） | AssignmentCompletionVO | 无 |
| getStudentPerformance | 获取学生成绩统计 | StudentPerformanceQueryDTO（courseId, studentId） | StudentPerformanceVO | 无 |
| generateChart | 生成图表数据 | ChartQueryDTO（chartType, dataType, params） | ChartDataVO | 不支持的图表类型异常、参数错误异常 |
| generateReport | 生成分析报告 | ReportQueryDTO（reportType, params） | ReportVO | 不支持的报告类型异常、参数错误异常 |

#### 3.5.4 处理逻辑

##### 3.5.4.1 资源分布统计流程

1. 接收统计请求，验证输入参数
2. 查询资源数据，按类型、课程等维度分组
3. 计算各维度的资源数量和占比
4. 生成图表数据
5. 返回统计结果

##### 3.5.4.2 作业完成情况统计流程

1. 接收统计请求，验证输入参数
2. 查询作业提交数据
3. 计算提交率、未提交率、平均成绩等指标
4. 生成图表数据
5. 返回统计结果

#### 3.5.5 异常处理

| 异常类型 | 异常描述 | 处理方式 |
|---------|---------|----------|
| UnsupportedChartTypeException | 不支持的图表类型 | 返回400 Bad Request，提示不支持该图表类型 |
| InvalidParametersException | 参数错误 | 返回400 Bad Request，提示参数错误 |
| UnsupportedReportTypeException | 不支持的报告类型 | 返回400 Bad Request，提示不支持该报告类型 |

### 3.6 MongoDB双写模块

#### 3.6.1 模块概述

MongoDB双写模块负责实现MySQL和MongoDB的数据双写，包括数据同步、重试机制和垃圾数据清理。

#### 3.6.2 类图

```
├── MongoDbService              # MongoDB操作服务接口
├── MongoDbServiceImpl          # MongoDB操作服务实现
├── ResourceDocument            # 资源MongoDB文档
├── AssignmentDocument          # 作业MongoDB文档
├── SubmissionDocument          # 作业提交MongoDB文档
├── MongoCleanupJob             # MongoDB清理任务
├── MongoCleanup                # MongoDB清理任务实体
└── MongoCleanupMapper          # MongoDB清理任务数据访问接口
```

#### 3.6.3 核心接口

| 接口名称 | 功能描述 | 输入 | 输出 | 异常处理 |
|---------|---------|------|------|----------|
| saveResource | 保存资源到MongoDB | ResourceDocument | ResourceDocument | MongoDB操作失败异常 |
| saveAssignment | 保存作业到MongoDB | AssignmentDocument | AssignmentDocument | MongoDB操作失败异常 |
| saveSubmission | 保存作业提交到MongoDB | SubmissionDocument | SubmissionDocument | MongoDB操作失败异常 |
| deleteDocument | 删除MongoDB文档 | String mongoId, String collection | Boolean | MongoDB操作失败异常 |
| deleteDocumentWithRetry | 带重试机制的删除MongoDB文档 | String mongoId, String collection, int maxRetries | Boolean | MongoDB操作失败异常 |

#### 3.6.4 处理逻辑

##### 3.6.4.1 双写流程

1. 生成mongoId
2. 创建MongoDB文档并写入MongoDB
3. 创建MySQL实体并写入MySQL
4. 若MySQL写入成功，返回成功结果
5. 若MySQL写入失败，MongoDB数据成为垃圾数据，记录到清理表

##### 3.6.4.2 文档删除流程

1. 接收删除请求
2. 尝试删除MongoDB文档
3. 若删除成功，返回成功结果
4. 若删除失败，重试指定次数
5. 若重试失败，记录到清理表，由定时任务处理

##### 3.6.4.3 垃圾数据清理流程

1. 定时任务每小时执行一次
2. 查询状态为待清理的MongoCleanup记录
3. 遍历记录，尝试删除对应的MongoDB文档
4. 若删除成功，更新记录状态为已清理
5. 若删除失败，保留记录，等待下次清理

#### 3.6.5 异常处理

| 异常类型 | 异常描述 | 处理方式 |
|---------|---------|----------|
| MongoDbOperationException | MongoDB操作失败 | 记录日志，返回失败结果，必要时记录到清理表 |
| RetryExhaustedException | 重试次数耗尽 | 记录日志，返回失败结果，记录到清理表 |

## 4. 数据库设计

### 4.1 数据库概述

系统使用MySQL和MongoDB双写存储数据，其中MySQL存储结构化数据，MongoDB存储非结构化数据。

### 4.2 MySQL数据库设计

#### 4.2.1 用户表（user）

| 字段名 | 数据类型 | 约束 | 描述 |
|-------|---------|------|------|
| id | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 用户ID |
| username | VARCHAR(50) | UNIQUE, NOT NULL | 用户名 |
| password | VARCHAR(100) | NOT NULL | 密码（BCrypt加密） |
| email | VARCHAR(100) | UNIQUE, NOT NULL | 邮箱 |
| real_name | VARCHAR(50) | NOT NULL | 真实姓名 |
| role | VARCHAR(20) | NOT NULL | 角色（ADMIN/TEACHER/STUDENT） |
| status | INT | NOT NULL DEFAULT 1 | 状态（0-禁用，1-启用） |
| create_time | DATETIME | NOT NULL | 创建时间 |
| update_time | DATETIME | NOT NULL | 更新时间 |

#### 4.2.2 课程表（course）

| 字段名 | 数据类型 | 约束 | 描述 |
|-------|---------|------|------|
| id | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 课程ID |
| name | VARCHAR(100) | NOT NULL | 课程名称 |
| description | TEXT | | 课程描述 |
| teacher_id | BIGINT | NOT NULL, FOREIGN KEY | 教师ID |
| category_id | BIGINT | FOREIGN KEY | 分类ID |
| status | INT | NOT NULL DEFAULT 1 | 状态（0-禁用，1-启用） |
| create_time | DATETIME | NOT NULL | 创建时间 |
| update_time | DATETIME | NOT NULL | 更新时间 |

#### 4.2.3 资源表（resource）

| 字段名 | 数据类型 | 约束 | 描述 |
|-------|---------|------|------|
| id | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 资源ID |
| name | VARCHAR(255) | NOT NULL | 资源名称 |
| description | TEXT | | 资源描述 |
| file_path | VARCHAR(255) | NOT NULL | 文件路径 |
| file_name | VARCHAR(255) | NOT NULL | 文件名 |
| file_size | BIGINT | NOT NULL | 文件大小 |
| uploader_user_id | BIGINT | NOT NULL, FOREIGN KEY | 上传者ID |
| course_id | BIGINT | NOT NULL, FOREIGN KEY | 课程ID |
| download_count | INT | DEFAULT 0 | 下载次数 |
| type | VARCHAR(50) | NOT NULL | 资源类型 |
| status | INT | NOT NULL | 状态：0-禁用，1-启用 |
| create_time | DATETIME | NOT NULL | 创建时间 |
| update_time | DATETIME | NOT NULL | 更新时间 |
| mongo_id | VARCHAR(36) | NOT NULL | MongoDB文档ID |

#### 4.2.4 作业表（assignment）

| 字段名 | 数据类型 | 约束 | 描述 |
|-------|---------|------|------|
| id | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 作业ID |
| title | VARCHAR(255) | NOT NULL | 作业标题 |
| description | TEXT | | 作业描述 |
| content | TEXT | | 作业内容 |
| course_id | BIGINT | NOT NULL, FOREIGN KEY | 课程ID |
| teacher_id | BIGINT | NOT NULL, FOREIGN KEY | 教师ID |
| deadline | DATETIME | NOT NULL | 截止时间 |
| max_score | INT | NOT NULL DEFAULT 100 | 满分 |
| status | INT | NOT NULL | 状态：0-禁用，1-启用 |
| create_time | DATETIME | NOT NULL | 创建时间 |
| update_time | DATETIME | NOT NULL | 更新时间 |
| mongo_id | VARCHAR(36) | NOT NULL | MongoDB文档ID |

#### 4.2.5 作业提交表（homework_submission）

| 字段名 | 数据类型 | 约束 | 描述 |
|-------|---------|------|------|
| id | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 提交ID |
| assignment_id | BIGINT | NOT NULL, FOREIGN KEY | 作业ID |
| student_id | BIGINT | NOT NULL, FOREIGN KEY | 学生ID |
| student_name | VARCHAR(50) | NOT NULL | 学生姓名 |
| content | TEXT | | 作业内容 |
| attachment_url | VARCHAR(255) | | 附件URL |
| attachment_name | VARCHAR(255) | | 附件名称 |
| attachment_size | BIGINT | | 附件大小 |
| status | VARCHAR(20) | NOT NULL | 状态 |
| score | INT | | 分数 |
| comment | TEXT | | 评语 |
| feedback | TEXT | | 反馈 |
| submit_time | DATETIME | | 提交时间 |
| grade_time | DATETIME | | 批改时间 |
| create_time | DATETIME | NOT NULL | 创建时间 |
| update_time | DATETIME | NOT NULL | 更新时间 |
| mongo_id | VARCHAR(36) | NOT NULL | MongoDB文档ID |

#### 4.2.6 MongoDB清理表（mongo_cleanup）

| 字段名 | 数据类型 | 约束 | 描述 |
|-------|---------|------|------|
| id | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 清理ID |
| mongo_id | VARCHAR(36) | NOT NULL | MongoDB文档ID |
| collection | VARCHAR(50) | NOT NULL | 集合名称 |
| create_time | DATETIME | NOT NULL | 创建时间 |
| status | INT | NOT NULL DEFAULT 0 | 状态：0-待清理，1-已清理 |
| cleanup_time | DATETIME | | 清理时间 |

### 4.3 MongoDB文档设计

#### 4.3.1 资源文档（resource）

```json
{
  "_id": "f8b4c9d3-e6a2-4c5d-9f8e-7a6b5c4d3b2a", // UUID，与MySQL的mongo_id对应
  "resourceId": 1, // 与MySQL的resource.id对应
  "name": "课程资源",
  "description": "这是一个课程资源",
  "filePath": "/uploads/2024/05/20/file1.pdf",
  "fileName": "file1.pdf",
  "fileSize": 1024000,
  "uploaderUserId": 1,
  "uploaderName": "教师A",
  "courseId": 1,
  "courseName": "Java编程",
  "downloadCount": 10,
  "type": "pdf",
  "status": 1,
  "createTime": ISODate("2024-05-20T10:00:00Z"),
  "updateTime": ISODate("2024-05-20T10:00:00Z")
}
```

#### 4.3.2 作业文档（assignment）

```json
{
  "_id": "a1b2c3d4-e5f6-7g8h-9i0j-1k2l3m4n5o6p", // UUID，与MySQL的mongo_id对应
  "assignmentId": 1, // 与MySQL的assignment.id对应
  "title": "第一次作业",
  "description": "完成教材第1-2章练习题",
  "content": "详细作业要求...",
  "courseId": 1,
  "courseName": "Java编程",
  "teacherId": 1,
  "teacherName": "教师A",
  "deadline": ISODate("2024-05-30T23:59:59Z"),
  "maxScore": 100,
  "status": 1,
  "createTime": ISODate("2024-05-20T10:00:00Z"),
  "updateTime": ISODate("2024-05-20T10:00:00Z")
}
```

#### 4.3.3 作业提交文档（homework_submission）

```json
{
  "_id": "b2c3d4e5-f6g7-8h9i-0j1k-2l3m4n5o6p7q", // UUID，与MySQL的mongo_id对应
  "submissionId": 1, // 与MySQL的homework_submission.id对应
  "assignmentId": 1,
  "studentId": 1,
  "studentName": "学生A",
  "content": "作业答案...",
  "attachment": {
    "url": "/uploads/2024/05/28/answer.pdf",
    "name": "answer.pdf",
    "size": 512000
  },
  "status": "submitted",
  "score": 90,
  "comment": "优秀",
  "feedback": "继续保持",
  "submitTime": ISODate("2024-05-28T14:30:00Z"),
  "gradeTime": ISODate("2024-05-29T16:00:00Z"),
  "createTime": ISODate("2024-05-28T14:30:00Z"),
  "updateTime": ISODate("2024-05-29T16:00:00Z")
}
```

### 4.4 数据关系图

```
user 1:n course (teacher_id)
user 1:n resource (uploader_user_id)
course 1:n resource (course_id)
course 1:n assignment (course_id)
assignment 1:n homework_submission (assignment_id)
user 1:n homework_submission (student_id)
resource 1:1 resource_document (mongo_id)
assignment 1:1 assignment_document (mongo_id)
homework_submission 1:1 submission_document (mongo_id)
```

## 5. 接口设计

### 5.1 RESTful API设计原则

1. **资源命名**：使用名词表示资源，如`/users`、`/courses`、`/resources`
2. **HTTP方法**：
   - GET：获取资源
   - POST：创建资源
   - PUT：更新资源
   - DELETE：删除资源
   - PATCH：部分更新资源
3. **状态码**：
   - 200 OK：成功
   - 201 Created：创建成功
   - 204 No Content：无内容
   - 400 Bad Request：请求参数错误
   - 401 Unauthorized：未授权
   - 403 Forbidden：禁止访问
   - 404 Not Found：资源不存在
   - 500 Internal Server Error：服务器内部错误
4. **请求格式**：使用JSON格式
5. **响应格式**：使用统一的API响应格式

### 5.2 统一响应格式

```json
{
  "code": 200,
  "message": "成功",
  "data": {}
}
```

### 5.3 主要API接口

#### 5.3.1 用户管理API

| API路径 | 方法 | 功能描述 | 认证要求 | 权限要求 |
|---------|------|---------|----------|----------|
| /api/users/register | POST | 用户注册 | 否 | 否 |
| /api/users/login | POST | 用户登录 | 否 | 否 |
| /api/users/info | GET | 获取当前用户信息 | 是 | 是 |
| /api/users/{id} | GET | 获取用户信息 | 是 | 是 |
| /api/users/{id} | PUT | 更新用户信息 | 是 | 是 |
| /api/users/{id} | DELETE | 删除用户 | 是 | ADMIN |
| /api/users/{id}/change-password | POST | 修改密码 | 是 | 是 |
| /api/users/reset-password | POST | 重置密码 | 否 | 否 |
| /api/roles | GET | 获取角色列表 | 是 | ADMIN |
| /api/roles/{id}/permissions | POST | 分配权限 | 是 | ADMIN |

#### 5.3.2 课程管理API

| API路径 | 方法 | 功能描述 | 认证要求 | 权限要求 |
|---------|------|---------|----------|----------|
| /api/courses | GET | 获取课程列表 | 是 | 是 |
| /api/courses | POST | 创建课程 | 是 | ADMIN/TEACHER |
| /api/courses/{id} | GET | 获取课程详情 | 是 | 是 |
| /api/courses/{id} | PUT | 更新课程 | 是 | ADMIN/TEACHER |
| /api/courses/{id} | DELETE | 删除课程 | 是 | ADMIN/TEACHER |
| /api/courses/{id}/students | GET | 获取课程学生列表 | 是 | ADMIN/TEACHER |
| /api/courses/{id}/students | POST | 添加学生到课程 | 是 | ADMIN/TEACHER |
| /api/courses/{id}/students/{studentId} | DELETE | 从课程移除学生 | 是 | ADMIN/TEACHER |

#### 5.3.3 资源管理API

| API路径 | 方法 | 功能描述 | 认证要求 | 权限要求 |
|---------|------|---------|----------|----------|
| /api/resources | GET | 获取资源列表 | 是 | 是 |
| /api/resources | POST | 上传资源 | 是 | ADMIN/TEACHER |
| /api/resources/{id} | GET | 获取资源详情 | 是 | 是 |
| /api/resources/{id} | PUT | 更新资源 | 是 | ADMIN/TEACHER |
| /api/resources/{id} | DELETE | 删除资源 | 是 | ADMIN/TEACHER |
| /api/resources/{id}/download | GET | 下载资源 | 是 | 是 |
| /api/resources/{id}/preview | GET | 预览资源 | 是 | 是 |
| /api/resources/search | GET | 搜索资源 | 是 | 是 |
| /api/files/upload | POST | 文件上传 | 是 | 是 |
| /api/files/{filename} | GET | 文件下载 | 是 | 是 |

#### 5.3.4 作业管理API

| API路径 | 方法 | 功能描述 | 认证要求 | 权限要求 |
|---------|------|---------|----------|----------|
| /api/assignments | GET | 获取作业列表 | 是 | 是 |
| /api/assignments | POST | 创建作业 | 是 | ADMIN/TEACHER |
| /api/assignments/{id} | GET | 获取作业详情 | 是 | 是 |
| /api/assignments/{id} | PUT | 更新作业 | 是 | ADMIN/TEACHER |
| /api/assignments/{id} | DELETE | 删除作业 | 是 | ADMIN/TEACHER |
| /api/assignments/{id}/submissions | GET | 获取作业提交列表 | 是 | ADMIN/TEACHER |
| /api/submissions | POST | 提交作业 | 是 | STUDENT |
| /api/submissions/{id} | GET | 获取提交详情 | 是 | 是 |
| /api/submissions/{id}/grade | POST | 批改作业 | 是 | ADMIN/TEACHER |

#### 5.3.5 数据可视化API

| API路径 | 方法 | 功能描述 | 认证要求 | 权限要求 |
|---------|------|---------|----------|----------|
| /api/dashboard/resource-distribution | GET | 获取资源分布 | 是 | ADMIN/TEACHER |
| /api/dashboard/assignment-completion | GET | 获取作业完成情况 | 是 | ADMIN/TEACHER |
| /api/dashboard/student-performance | GET | 获取学生成绩统计 | 是 | ADMIN/TEACHER |
| /api/analytics/reports | POST | 生成分析报告 | 是 | ADMIN/TEACHER |
| /api/charts | POST | 生成图表数据 | 是 | ADMIN/TEACHER |

### 5.4 接口输入输出示例

#### 5.4.1 用户登录API

**请求**：
```json
POST /api/users/login
Content-Type: application/json

{
  "username": "admin",
  "password": "admin123"
}
```

**响应**：
```json
{
  "code": 200,
  "message": "成功",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "refreshToken": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "expireTime": 1622505600000
  }
}
```

#### 5.4.2 资源上传API

**请求**：
```multipart/form-data
POST /api/resources
Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

file: [文件]
name: 课程资源
description: 这是一个课程资源
courseId: 1
type: pdf
```

**响应**：
```json
{
  "code": 200,
  "message": "成功",
  "data": {
    "id": 1,
    "name": "课程资源",
    "description": "这是一个课程资源",
    "filePath": "/uploads/2024/05/20/file1.pdf",
    "fileName": "file1.pdf",
    "fileSize": 1024000,
    "uploaderUserId": 1,
    "courseId": 1,
    "downloadCount": 0,
    "type": "pdf",
    "status": 1,
    "createTime": "2024-05-20 10:00:00",
    "updateTime": "2024-05-20 10:00:00",
    "mongoId": "f8b4c9d3-e6a2-4c5d-9f8e-7a6b5c4d3b2a"
  }
}
```

## 6. 安全设计

### 6.1 认证机制

#### 6.1.1 JWT认证

- 使用HS256算法签名JWT Token
- Token有效期24小时，支持刷新机制
- Token包含用户信息和权限，减少数据库查询
- 实现Token黑名单机制，支持强制登出

#### 6.1.2 刷新Token机制

- 刷新Token有效期7天
- 刷新Token只能使用一次，使用后立即失效，生成新的刷新Token
- 刷新Token存储在数据库中，支持失效管理

### 6.2 授权机制

#### 6.2.1 基于角色的访问控制

- 细粒度的权限控制，支持接口级权限验证
- 动态分配和修改角色权限
- 前端路由权限控制，防止未授权访问

#### 6.2.2 权限设计

| 角色 | 权限范围 |
|------|----------|
| ADMIN | 所有功能权限 |
| TEACHER | 课程管理、资源管理、作业管理、数据可视化 |
| STUDENT | 课程查看、资源查看与下载、作业提交与查看 |

### 6.3 数据安全

#### 6.3.1 数据加密

- 敏感数据传输采用HTTPS
- 密码使用BCrypt加密存储
- 敏感数据存储采用AES-256加密
- 支持数据脱敏，保护用户隐私

#### 6.3.2 数据备份

- 定期备份数据库，备份策略：
  - 全量备份：每天一次
  - 增量备份：每小时一次
- 备份数据存储在安全的位置，支持异地备份
- 定期测试数据恢复，确保备份有效性

### 6.4 防护措施

#### 6.4.1 SQL注入防护

- 使用MyBatis-Plus的参数化查询
- 输入验证和过滤
- 禁止直接拼接SQL语句
- 最小权限原则配置数据库用户

#### 6.4.2 XSS防护

- 输入验证和过滤
- 输出编码
- 使用Content-Security-Policy头
- 启用浏览器XSS防护

#### 6.4.3 CSRF防护

- 使用JWT Token，无状态认证
- 验证Origin和Referer头
- 使用SameSite Cookie属性
- 避免使用Cookie存储敏感信息

#### 6.4.4 暴力破解防护

- 登录失败次数限制，超过5次锁定账号15分钟
- 使用Google reCAPTCHA验证码
- 登录IP限制，防止异地登录
- 记录登录日志，异常登录告警

## 7. 性能设计

### 7.1 缓存设计

#### 7.1.1 二级缓存架构

- **一级缓存（Caffeine）**：
  - 应用进程内缓存
  - 热点数据存储
  - 最大容量：1000条
  - 过期时间：30分钟
  - 访问响应时间≤10ms

- **二级缓存（Redis）**：
  - 分布式共享缓存
  - 持久化存储
  - 最大内存：根据服务器配置调整
  - 过期时间：60分钟
  - 访问响应时间≤50ms

#### 7.1.2 缓存策略

- **缓存命中率**：≥80%
- **缓存更新策略**：
  - 写入更新：修改数据时同时更新缓存
  - 过期失效：设置合理的过期时间
  - 主动失效：重要数据修改时主动失效缓存
- **缓存穿透处理**：
  - 空值缓存
  - 布隆过滤器（预留）

### 7.2 数据库性能优化

#### 7.2.1 索引优化

- 为频繁查询的字段创建索引
- 避免创建过多索引，影响写入性能
- 定期优化索引，删除无用索引
- 使用复合索引提高查询效率

#### 7.2.2 查询优化

- 避免全表扫描
- 使用分页查询，限制返回数据量
- 优化SQL语句，减少连接操作
- 使用MyBatis-Plus的优化查询方法

#### 7.2.3 连接池优化

- 合理配置连接池参数：
  - 最小连接数：CPU核心数
  - 最大连接数：CPU核心数×2+有效磁盘数
  - 连接超时时间：30秒
  - 空闲连接超时时间：180秒

### 7.3 前端性能优化

- 路由懒加载，减少首屏加载时间
- 组件按需加载，减少打包体积
- 图片懒加载，提高页面加载速度
- 使用CDN加速静态资源访问
- 减少HTTP请求，合并CSS和JavaScript文件
- 启用GZIP压缩，减少传输数据量

### 7.4 后端性能优化

- 使用异步处理非核心业务逻辑
- 合理使用线程池，提高并发处理能力
- 减少数据库连接和网络请求
- 优化序列化和反序列化
- 使用响应式编程（预留）

### 7.5 性能监控

- 使用Spring Boot Actuator监控应用状态
- 集成Prometheus和Grafana，实时监控性能指标
- 监控关键指标：
  - 响应时间
  - 并发用户数
  - 吞吐量
  - 错误率
  - 缓存命中率
  - 数据库连接数

## 8. 部署设计

### 8.1 部署环境

#### 8.1.1 开发环境

| 组件 | 版本 | 配置 |
|------|------|------|
| Java | 1.8+ | 8GB内存 |
| MySQL | 8.0+ | 4GB内存，50GB磁盘 |
| MongoDB | 4.4+ | 4GB内存，50GB磁盘 |
| Redis | 6.0+ | 2GB内存 |
| Node.js | 14.0+ | 4GB内存 |

#### 8.1.2 生产环境

| 组件 | 版本 | 配置 |
|------|------|------|
| Java | 1.8+ | 16GB+内存 |
| MySQL | 8.0+ | 8GB+内存，200GB+磁盘，主从复制 |
| MongoDB | 4.4+ | 8GB+内存，200GB+磁盘，副本集 |
| Redis | 6.0+ | 4GB+内存，主从复制 |
| Nginx | 1.24+ | 4GB+内存 |
| Docker | 20.10+ | 无 |
| Kubernetes | 1.20+ | 无 |

### 8.2 部署架构

```
用户 → Nginx → API网关 → 后端服务集群 → MySQL主从 → Redis主从
                                      ↓
                                      → MongoDB副本集
```

### 8.3 部署流程

#### 8.3.1 后端部署

1. 克隆代码仓库
2. 配置环境变量和配置文件
3. 构建项目：`mvn clean package -DskipTests`
4. 部署jar包到服务器
5. 启动应用：`java -jar resource-sharing-1.0-SNAPSHOT.jar`
6. 配置Nginx反向代理
7. 启动Nginx服务

#### 8.3.2 前端部署

1. 克隆代码仓库
2. 配置API地址
3. 安装依赖：`npm install`
4. 构建项目：`npm run build`
5. 部署dist目录到Nginx服务器
6. 配置Nginx静态资源访问

#### 8.3.3 数据库部署

1. 安装MySQL和MongoDB
2. 配置数据库参数
3. 创建数据库和用户
4. 导入初始化数据
5. 配置主从复制/副本集

### 8.4 容器化部署

使用Docker和Docker Compose实现容器化部署：

1. 编写Dockerfile
2. 编写docker-compose.yml
3. 构建镜像：`docker-compose build`
4. 启动服务：`docker-compose up -d`

### 8.5 持续集成/持续部署

使用Jenkins实现CI/CD：

1. 配置代码仓库钩子
2. 编写Jenkinsfile
3. 实现自动构建、测试、部署
4. 集成SonarQube进行代码质量检查
5. 集成Docker实现容器化部署

## 9. 测试计划

### 9.1 测试策略

1. **单元测试**：测试每个模块的核心功能
2. **集成测试**：测试模块之间的集成
3. **系统测试**：测试整个系统的功能和性能
4. **回归测试**：确保修改不会引入新问题
5. **性能测试**：测试系统的响应时间、并发用户数、吞吐量
6. **安全测试**：测试系统的安全性，包括认证授权、SQL注入、XSS等

### 9.2 测试工具

| 测试类型 | 工具 |
|---------|------|
| 单元测试 | JUnit、Mockito |
| 集成测试 | Spring Boot Test |
| API测试 | Postman、Swagger |
| 性能测试 | JMeter、Gatling |
| 安全测试 | OWASP ZAP、SonarQube |
| 自动化测试 | Selenium、TestNG |

### 9.3 测试用例设计

#### 9.3.1 用户管理测试

| 测试用例 | 输入 | 预期输出 |
|---------|------|----------|
| 用户注册 | 用户名、密码、邮箱、角色 | 注册成功，返回用户信息 |
| 用户登录 | 正确的用户名和密码 | 登录成功，返回Token |
| 用户登录 | 错误的用户名或密码 | 登录失败，返回错误信息 |
| 获取用户信息 | 有效的用户ID | 返回用户信息 |
| 更新用户信息 | 用户ID、更新数据 | 更新成功，返回更新后的用户信息 |
| 删除用户 | 有效的用户ID | 删除成功 |

#### 9.3.2 资源管理测试

| 测试用例 | 输入 | 预期输出 |
|---------|------|----------|
| 上传资源 | 文件、资源信息 | 上传成功，返回资源信息 |
| 下载资源 | 有效的资源ID | 下载成功，返回文件流 |
| 获取资源详情 | 有效的资源ID | 返回资源信息 |
| 更新资源 | 资源ID、更新数据 | 更新成功，返回更新后的资源信息 |
| 删除资源 | 有效的资源ID | 删除成功 |
| 搜索资源 | 关键词 | 返回匹配的资源列表 |

#### 9.3.3 作业管理测试

| 测试用例 | 输入 | 预期输出 |
|---------|------|----------|
| 创建作业 | 作业信息 | 创建成功，返回作业信息 |
| 提交作业 | 作业ID、提交内容 | 提交成功，返回提交信息 |
| 批改作业 | 提交ID、评分信息 | 批改成功，返回批改后的提交信息 |
| 获取作业列表 | 课程ID | 返回课程的作业列表 |
| 获取作业提交列表 | 作业ID | 返回作业的提交列表 |

### 9.4 性能测试要点

1. **响应时间测试**：
   - 页面加载时间≤2秒
   - API请求响应时间≤1秒
   - 文件上传/下载速度≥1MB/s

2. **并发测试**：
   - 支持500并发用户
   - 系统在峰值负载下稳定运行
   - 无明显性能下降

3. **吞吐量测试**：
   - 每秒处理API请求≥100个
   - 每秒处理文件上传≥10个

4. **稳定性测试**：
   - 系统连续运行72小时无故障
   - 内存泄漏检测
   - 数据库连接泄漏检测

### 9.5 安全测试要点

1. **认证测试**：
   - 测试不同角色的登录
   - 测试Token失效和刷新
   - 测试无效Token访问

2. **授权测试**：
   - 测试越权访问
   - 测试权限继承
   - 测试权限修改

3. **漏洞测试**：
   - SQL注入测试
   - XSS测试
   - CSRF测试
   - 命令注入测试

4. **数据安全测试**：
   - 敏感数据加密测试
   - 数据备份恢复测试
   - 数据脱敏测试

### 9.6 测试报告

测试完成后，生成详细的测试报告，包括：

1. 测试概述
2. 测试环境
3. 测试用例执行情况
4. 缺陷统计和分析
5. 性能测试结果
6. 安全测试结果
7. 测试结论和建议

## 10. 总结

本详细设计文档全面描述了课程资源共享平台的设计实现，包括总体架构、模块设计、数据库设计、接口设计、安全设计、性能设计、部署设计和测试计划。文档详细、具体，可直接指导开发团队实施。

文档遵循了清晰、一致的术语，避免了模糊表述，确保每个设计点都有明确依据。关键流程使用了流程图或时序图表示，每个功能模块都包含了输入输出、处理逻辑和异常处理。

通过本设计文档，开发团队可以清晰地了解系统的设计思路和实现方案，按照文档进行开发，确保系统的质量和一致性。同时，文档也为测试团队、运维团队和项目管理人员提供了重要的参考依据。