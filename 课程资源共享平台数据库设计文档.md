# 课程资源共享平台数据库设计文档

## 1. 文档元数据

| 项目 | 内容 |
|------|------|
| 文档名称 | 课程资源共享平台数据库设计文档 |
| 版本号 | 1.0 |
| 设计日期 | 2025-11-04 |
| 设计者 | 刘希夷 |
| 适用项目 | 课程资源共享平台 |
| 数据库类型 | MySQL 8.0.34 |

## 2. 数据库概述

课程资源共享平台数据库用于存储和管理平台的核心数据，包括用户信息、课程信息、资源信息、作业信息以及相关关联数据。数据库采用关系型设计，通过外键约束确保数据完整性，并使用索引优化查询性能。同时，系统采用MySQL和MongoDB双写架构，将部分数据同步到MongoDB以提高查询性能和扩展性。

## 3. 数据库对象设计

### 3.1 表结构设计

#### 3.1.1 用户表（user）

**用途**：存储平台所有用户信息，包括管理员、教师和学生。

| 字段名 | 数据类型 | 约束 | 描述 |
|--------|----------|------|------|
| id | bigint | 主键，自增 | 用户ID |
| username | varchar(50) | 唯一，非空 | 用户名 |
| password | varchar(100) | 非空 | 密码 |
| role | varchar(20) | 非空 | 角色：ADMIN/TEACHER/STUDENT |
| real_name | varchar(50) |  | 真实姓名 |
| classname | varchar(255) |  | 班级名称 |
| email | varchar(100) |  | 邮箱 |
| create_time | datetime | 默认CURRENT_TIMESTAMP | 创建时间 |
| update_time | datetime | 默认CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |
| phone | varchar(20) |  | 手机号码 |
| avatar | varchar(255) |  | 头像URL |
| enabled | tinyint(1) | 默认1 | 是否启用 |
| last_login_time | datetime |  | 最后登录时间 |

**索引**：
- 唯一索引：uk_username (username)

**初始数据**：
- 系统管理员：admin/admin123

#### 3.1.2 课程表（course）

**用途**：存储课程基本信息。

| 字段名 | 数据类型 | 约束 | 描述 |
|--------|----------|------|------|
| id | bigint | 主键，自增 | 课程ID |
| name | varchar(100) | 非空 | 课程名称 |
| description | text |  | 课程描述 |
| teacher_id | bigint | 非空，外键（user.id） | 教师ID |
| create_time | datetime | 默认CURRENT_TIMESTAMP | 创建时间 |
| update_time | datetime | 默认CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

**索引**：
- 普通索引：idx_teacher (teacher_id)

**外键关系**：
- teacher_id → user.id（ON DELETE RESTRICT, ON UPDATE RESTRICT）

#### 3.1.3 学生课程关联表（student_course）

**用途**：记录学生与课程的关联关系，实现多对多映射。

| 字段名 | 数据类型 | 约束 | 描述 |
|--------|----------|------|------|
| id | bigint | 主键，自增 | 关联ID |
| student_id | bigint | 非空，外键（user.id） | 学生ID |
| course_id | bigint | 非空，外键（course.id） | 课程ID |
| create_time | datetime | 默认CURRENT_TIMESTAMP | 创建时间 |

**索引**：
- 唯一索引：uk_student_course (student_id, course_id)
- 普通索引：idx_course (course_id)

**外键关系**：
- student_id → user.id（ON DELETE RESTRICT, ON UPDATE RESTRICT）
- course_id → course.id（ON DELETE RESTRICT, ON UPDATE RESTRICT）

#### 3.1.4 资源表（resource）

**用途**：存储课程资源信息，包括文件名称、路径、大小等。

| 字段名 | 数据类型 | 约束 | 描述 |
|--------|----------|------|------|
| id | bigint | 主键，自增 | 资源ID |
| name | varchar(255) | 非空 | 资源名称 |
| description | text |  | 资源描述 |
| file_path | varchar(255) | 非空 | 文件路径 |
| file_name | varchar(255) | 非空 | 文件名 |
| file_size | bigint | 非空 | 文件大小（字节） |
| uploader_user_id | bigint | 非空，外键（user.id） | 上传者ID |
| course_id | bigint | 非空，外键（course.id） | 课程ID |
| download_count | int | 默认0 | 下载次数 |
| type | varchar(50) |  | 资源类型 |
| status | int | 默认1 | 状态：0-禁用，1-启用 |
| create_time | datetime | 默认CURRENT_TIMESTAMP | 创建时间 |
| update_time | datetime | 默认CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |
| mongo_id | varchar(36) | 非空 | MongoDB文档ID |

**索引**：
- 普通索引：idx_course_id (course_id)
- 普通索引：idx_uploader_id (uploader_user_id)

**外键关系**：
- course_id → course.id（ON DELETE RESTRICT, ON UPDATE RESTRICT）
- uploader_user_id → user.id（ON DELETE RESTRICT, ON UPDATE RESTRICT）

#### 3.1.5 作业表（assignment）

**用途**：存储作业基本信息。

| 字段名 | 数据类型 | 约束 | 描述 |
|--------|----------|------|------|
| id | bigint | 主键，自增 | 作业ID |
| title | varchar(255) | 非空 | 作业标题 |
| description | text |  | 作业描述 |
| course_id | bigint | 非空，外键（course.id） | 所属课程ID |
| teacher_id | bigint | 非空，外键（user.id） | 教师ID |
| deadline | datetime | 非空 | 截止日期 |
| create_time | datetime | 默认CURRENT_TIMESTAMP | 创建时间 |
| update_time | datetime | 默认CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |
| status | varchar(20) | 默认PENDING | 状态：PENDING-待完成，SUBMITTED-已提交，OVERDUE-已逾期 |
| mongo_id | varchar(36) | 非空 | MongoDB文档ID |

**索引**：
- 普通索引：idx_course_id (course_id)
- 普通索引：idx_teacher_id (teacher_id)
- 普通索引：idx_create_time (create_time)

**外键关系**：
- course_id → course.id（ON DELETE CASCADE, ON UPDATE RESTRICT）
- teacher_id → user.id（ON DELETE RESTRICT, ON UPDATE RESTRICT）

#### 3.1.6 作业提交表（homework_submission）

**用途**：存储学生作业提交信息。

| 字段名 | 数据类型 | 约束 | 描述 |
|--------|----------|------|------|
| attachment_name | varchar(255) |  | 附件原始文件名 |
| id | bigint | 主键，自增 | 提交ID |
| assignment_id | bigint | 外键（assignment.id） | 作业ID |
| student_id | bigint | 外键（user.id） | 学生ID |
| student_name | varchar(50) |  | 学生姓名 |
| content | text |  | 提交内容 |
| attachment_url | varchar(255) |  | 附件URL |
| attachment_size | bigint |  | 附件大小（字节） |
| score | int |  | 分数 |
| comment | text |  | 教师评语 |
| feedback | text |  | 反馈内容 |
| status | varchar(20) |  | 状态 |
| submit_time | datetime |  | 提交时间 |
| grade_time | datetime |  | 批改时间 |
| update_time | datetime |  | 更新时间 |
| create_time | datetime | 默认CURRENT_TIMESTAMP | 创建时间 |
| mongo_id | varchar(36) | 非空 | MongoDB文档ID |

**外键关系**：
- assignment_id → assignment.id（隐式，无ON DELETE/UPDATE规则）
- student_id → user.id（隐式，无ON DELETE/UPDATE规则）

#### 3.1.7 MongoDB清理记录表（mongo_cleanup）

**用途**：记录需要清理的MongoDB文档信息，用于定时清理MongoDB中的垃圾数据。

| 字段名 | 数据类型 | 约束 | 描述 |
|--------|----------|------|------|
| id | bigint | 主键，自增 | 清理ID |
| mongo_id | varchar(36) | 非空 | MongoDB文档ID |
| collection | varchar(50) | 非空 | 集合名称 |
| create_time | datetime | 非空 | 创建时间 |
| status | int | 默认0 | 状态：0-待清理，1-已清理 |
| cleanup_time | datetime |  | 清理时间 |

**索引**：
- 普通索引：idx_status (status)

### 3.2 索引设计

| 表名 | 索引名 | 索引类型 | 索引字段 | 用途 |
|------|--------|----------|----------|------|
| user | uk_username | 唯一索引 | username | 保证用户名唯一，加速用户登录查询 |
| course | idx_teacher | 普通索引 | teacher_id | 加速教师查询其课程列表 |
| student_course | uk_student_course | 唯一索引 | student_id, course_id | 保证学生与课程的唯一关联 |
| student_course | idx_course | 普通索引 | course_id | 加速查询课程的所有学生 |
| resource | idx_course_id | 普通索引 | course_id | 加速查询课程资源列表 |
| resource | idx_uploader_id | 普通索引 | uploader_user_id | 加速查询用户上传的资源 |
| assignment | idx_course_id | 普通索引 | course_id | 加速查询课程作业列表 |
| assignment | idx_teacher_id | 普通索引 | teacher_id | 加速教师查询其发布的作业 |
| assignment | idx_create_time | 普通索引 | create_time | 加速按创建时间排序查询作业 |
| mongo_cleanup | idx_status | 普通索引 | status | 加速查询待清理的MongoDB文档 |

### 3.3 表关系图

```
+----------------+      +----------------+      +-------------------+
|     user       |      |    course      |      |  student_course   |
+----------------+      +----------------+      +-------------------+
| id (PK)        |<-----+ teacher_id (FK)|<-----+ course_id (FK)    |
| username       |      | name           |      | student_id (FK)   |----->+----------------+
| password       |      | description    |      +-------------------+      |     user       |
| role           |      +----------------+                                 +----------------+
| real_name      |                                                         | id (PK)        |
+----------------+                                                         | role = STUDENT |
        ^                                                                  +----------------+
        |
        |
+----------------+      +----------------+      +-----------------------+
| resource       |      |  assignment    |      | homework_submission   |
+----------------+      +----------------+      +-----------------------+
| id (PK)        |      | id (PK)        |<-----+ assignment_id (FK)    |
| course_id (FK) |----->| course_id (FK) |      | student_id (FK)       |----->+----------------+
| uploader_user_id (FK)|<-----+ teacher_id (FK)|      | student_name       |      |     user       |
| mongo_id       |      | mongo_id       |      | mongo_id            |      +----------------+
+----------------+      +----------------+      +-----------------------+
        ^                          ^                          ^
        |                          |                          |
        +--------------------------+--------------------------+
                                     |
                                     v
+------------------------+
|  mongo_cleanup         |
+------------------------+
| id (PK)                |
| mongo_id               |
| collection             |
| create_time            |
| status                 |
| cleanup_time           |
+------------------------+
```

## 4. MongoDB双写设计

### 4.1 设计背景

为了提高系统的查询性能和扩展性，系统采用MySQL和MongoDB双写架构。核心业务数据存储在MySQL中，同时将部分查询频繁的数据同步到MongoDB中，以利用MongoDB的高性能查询能力。

### 4.2 双写数据范围

目前实现双写的表包括：
- resource（资源表）
- assignment（作业表）
- homework_submission（作业提交表）

### 4.3 双写机制

1. **写入流程**：
   - 生成MongoDB文档ID
   - 将数据写入MongoDB
   - 将数据写入MySQL（包含MongoDB文档ID）
   - 更新MongoDB文档（添加MySQL ID）

2. **更新流程**：
   - 生成新的MongoDB文档ID
   - 创建新的MongoDB文档
   - 更新MySQL数据（包含新的MongoDB文档ID）
   - 将旧的MongoDB文档ID加入清理队列

3. **删除流程**：
   - 删除MySQL数据
   - 将对应的MongoDB文档ID加入清理队列

### 4.4 垃圾数据清理

- **清理机制**：定时任务每小时执行一次，清理MongoDB中标记为待清理的文档
- **清理策略**：
  - 从mongo_cleanup表中查询状态为0（待清理）的记录
  - 根据记录中的collection和mongo_id删除MongoDB中的对应文档
  - 将清理成功的记录状态更新为1（已清理），并记录清理时间
  - 清理失败的记录会在下次定时任务中重试

## 5. 数据库性能优化

1. **索引优化**：
   - 为查询频繁的字段创建索引
   - 复合索引的字段顺序根据查询条件优化
   - 定期检查和优化索引使用情况

2. **查询优化**：
   - 避免全表扫描
   - 合理使用JOIN操作
   - 限制查询结果集大小
   - 使用分页查询

3. **写入优化**：
   - 批量写入操作
   - 异步写入MongoDB
   - 合理设置事务隔离级别

4. **存储优化**：
   - 定期清理无用数据
   - 合理设计数据类型
   - 分表分库策略（预留）

## 6. 数据库安全设计

1. **访问控制**：
   - 最小权限原则分配数据库用户权限
   - 定期更换数据库密码
   - 限制数据库访问IP

2. **数据加密**：
   - 敏感数据（如密码）加密存储
   - 传输过程中使用SSL加密

3. **备份策略**：
   - 定期全量备份
   - 增量备份
   - 异地备份

4. **审计日志**：
   - 记录重要操作日志
   - 定期审计数据库访问日志

## 7. 数据库部署与维护

### 7.1 部署环境

- **操作系统**：Linux/Unix
- **数据库版本**：MySQL 8.0.34
- **存储引擎**：InnoDB
- **字符集**：utf8mb4
- **排序规则**：utf8mb4_0900_ai_ci

### 7.2 维护计划

1. **日常维护**：
   - 监控数据库运行状态
   - 检查慢查询日志
   - 优化查询语句

2. **定期维护**：
   - 每周：优化表结构和索引
   - 每月：全量备份数据库
   - 每季度：数据库性能评估和调优
   - 每年：数据库架构评审和优化

## 8. 文档变更记录

| 版本 | 变更日期 | 变更内容 | 变更人 |
|------|----------|----------|--------|
| 1.0 | 2025-11-04 | 初始版本 | 刘希夷 |

## 9. 附录

### 9.1 数据库初始化脚本

数据库初始化脚本位于：`backend/db/course_resource_sharing.sql`

### 9.2 数据字典

| 表名 | 中文名称 | 核心业务 |
|------|----------|----------|
| user | 用户表 | 用户管理 |
| course | 课程表 | 课程管理 |
| student_course | 学生课程关联表 | 选课管理 |
| resource | 资源表 | 资源管理 |
| assignment | 作业表 | 作业管理 |
| homework_submission | 作业提交表 | 作业提交管理 |
| mongo_cleanup | MongoDB清理记录表 | MongoDB数据同步管理 |

### 9.3 命名规范

1. **表名**：使用小写字母，单词之间用下划线分隔，采用名词复数形式
2. **字段名**：使用小写字母，单词之间用下划线分隔
3. **索引名**：
   - 唯一索引：uk_表名_字段名
   - 普通索引：idx_表名_字段名
4. **外键名**：fk_表名_关联表名

### 9.4 数据类型使用规范

1. **字符串**：
   - 固定长度：char
   - 可变长度：varchar
   - 大文本：text

2. **数值**：
   - 整数：int, bigint
   - 小数：decimal

3. **日期时间**：
   - datetime：存储年月日时分秒
   - timestamp：存储时间戳

4. **布尔值**：
   - tinyint(1)：0-false，1-true

## 10. 总结

本数据库设计文档详细描述了课程资源共享平台的数据库结构、表关系、索引设计、MongoDB双写机制、性能优化、安全设计以及部署维护策略。该设计充分考虑了系统的功能性、性能、安全性和可扩展性，能够满足课程资源共享平台的业务需求。

随着系统业务的发展和用户量的增长，数据库设计可能需要进一步优化和调整，如分表分库、读写分离等。建议定期对数据库进行性能评估和优化，确保系统的稳定运行和良好的用户体验。